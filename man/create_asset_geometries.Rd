% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geospatial__create_asset_geometries.R
\name{create_asset_geometries}
\alias{create_asset_geometries}
\title{Create asset geometries from latitude/longitude coordinates}
\usage{
create_asset_geometries(
  assets_df,
  default_buffer_size_m = 1111,
  output_crs = 4326
)
}
\arguments{
\item{assets_df}{Data frame with asset information including latitude and longitude columns}

\item{default_buffer_size_m}{Numeric. Default buffer size in meters for point geometries when size_in_m2 is not available (default: 1111)}

\item{output_crs}{Character or numeric. Output CRS for the geometries (default: 4326 for WGS84). Can be EPSG code or proj4string.}
}
\value{
Data frame with original columns plus geometry (polygon) and centroid (point) columns in the specified output CRS
}
\description{
Creates point-based geometries with buffers for assets that have coordinate information.
This is a simplified version that only handles coordinate-based geolocation. Assets without
coordinates should use precomputed administrative hazard data instead.
}
\details{
Create geometries for assets using coordinates only
}
\examples{
\dontrun{
base_dir <- system.file("tests_data", package = "climate.risk.tool")
assets <- read_assets(base_dir)
# Filter to assets with coordinates
assets_with_coords <- assets |> dplyr::filter(!is.na(latitude), !is.na(longitude))
assets_geo <- create_asset_geometries(assets_with_coords)
}
}
