% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils__validate_inputs.R
\name{validate_input_coherence}
\alias{validate_input_coherence}
\title{Validate all input data for coherence and consistency}
\usage{
validate_input_coherence(
  assets_df,
  companies_df,
  hazards_dir,
  hazard_configs,
  cnae_exposure_df,
  precomputed_hazards_df = NULL,
  adm1_names,
  adm2_names,
  adm1_codes = NULL,
  adm2_codes = NULL,
  adm1_shape_ids = NULL,
  adm2_shape_ids = NULL,
  events_df = NULL
)
}
\arguments{
\item{assets_df}{Assets data frame}

\item{companies_df}{Companies data frame}

\item{cnae_exposure_df}{CNAE exposure data frame}

\item{precomputed_hazards_df}{Optional precomputed hazards data frame}

\item{adm1_names}{Character vector of valid ADM1 (province) names (ASCII-normalized)}

\item{adm2_names}{Character vector of valid ADM2 (municipality) names (ASCII-normalized)}

\item{events_df}{Optional events data frame to validate hazard-specific coverage}

\item{damage_factors_df}{Damage factors data frame}
}
\value{
List with validation results containing \code{errors} and \code{warnings} character vectors.
Stops execution if errors are found.
}
\description{
Performs comprehensive validation checks on input data including:
\itemize{
\item Province names in damage factors match ADM1 boundaries (after normalization)
\item Province and municipality names in assets match ADM1/ADM2 boundaries
\item Province and municipality names in precomputed hazards match ADM1/ADM2
\item CNAE codes in assets exist in reference CNAE file
\item Share of economic activity sums to 1 for each company
}
}
\details{
Validate Input Data Coherence
}
\examples{
\dontrun{
# Load boundary names
adm1 <- load_adm1_state_names("tests/tests_data")
adm2 <- load_adm2_municipality_names("tests/tests_data")
cnae_exposure <- load_mapping_from_config("tests/tests_data", load_hazard_configs("tests/tests_data/hazards/config"), "Heat", "cnae_exposure")
# Validate
validate_input_coherence(assets, companies, "tests/tests_data/hazards/config", load_hazard_configs("tests/tests_data/hazards/config"), cnae_exposure, precomputed_hazards, adm1, adm2)
}
}
