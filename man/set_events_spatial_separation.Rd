% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils__events_spatial_separation.R
\name{set_events_spatial_separation}
\alias{set_events_spatial_separation}
\title{Set per-event spatial separation filters}
\usage{
set_events_spatial_separation(
  events,
  spatial_filters = NULL,
  hazard_configs = NULL,
  strict = TRUE
)
}
\arguments{
\item{events}{Data frame of events.}

\item{spatial_filters}{Named list keyed by \code{event_id}.
Each entry can use:
\itemize{
\item shorthand: \code{list(state = c("11", "33"))}, \code{list(micro = c("R1"))}
\item explicit: \code{list(level = "state", codes = c("11", "33"), labels = c("Rondonia", "Rio de Janeiro"), scheme = "adm_regions")}
}}

\item{hazard_configs}{Optional named hazard config list (used to derive default scheme).}

\item{strict}{Logical. If \code{TRUE}, unknown \code{event_id} in filters throws an error.}
}
\value{
Events data frame with \code{spatial_level}, \code{spatial_region_codes},
\code{spatial_region_labels}, and \code{spatial_scheme}.
}
\description{
Programmatic helper to define spatial separation for selected events.
If \code{spatial_filters} is \code{NULL}, the input is returned unchanged so
\code{compute_risk()} can apply its default behavior (no spatial separation
when spatial columns are absent).
}
