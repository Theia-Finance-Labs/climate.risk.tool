% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils__read_hazards_and_mapping.R
\name{read_precomputed_hazards}
\alias{read_precomputed_hazards}
\title{Read precomputed hazard statistics for provinces and municipalities}
\usage{
read_precomputed_hazards(base_dir, hazard_configs = NULL)
}
\arguments{
\item{base_dir}{Character string specifying the base directory. The function looks for precomputed_adm_indicators.csv in base_dir/hazards/}

\item{hazard_configs}{Optional named list of hazard configs. If NULL, they are loaded from base_dir/hazards/config.}
}
\value{
tibble with precomputed hazard statistics including columns: adm_name, adm_code, shape_id,
state_code, adm_level, scenario_name, return_period, hazard_type, hazard_indicator,
hazard_name, aggregation_method, hazard_value. adm_level is "ADM1" for provinces
or "ADM2" for municipalities.
}
\description{
Reads precomputed hazard statistics from CSV file containing hazard data
aggregated at ADM1 (province) and ADM2 (municipality) levels. Used to look up hazard
values for assets without coordinates but with province or municipality information.
The file uses indicator metadata (\code{indicator_file}, \code{indicator_variable}) which are mapped
to \code{hazard_type} and \code{hazard_indicator} using the hazard config YAML files.
}
\details{
Read precomputed administrative hazard statistics from CSV file
}
\examples{
\dontrun{
base_dir <- system.file("tests_data", package = "climate.risk.tool")
precomputed <- read_precomputed_hazards(base_dir)
# Look up Amazonas province flood hazard
amazonas_flood <- precomputed |>
  dplyr::filter(adm_name == "Amazonas", adm_level == "ADM1", hazard_type == "flood")
}
}
