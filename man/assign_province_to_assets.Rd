% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils__read_inputs.R
\name{assign_province_to_assets}
\alias{assign_province_to_assets}
\title{Assign provinces to assets using spatial matching (loads boundaries from base_dir)}
\usage{
assign_province_to_assets(assets_df, base_dir)
}
\arguments{
\item{assets_df}{Data frame with asset information}

\item{base_dir}{Base directory containing areas subdirectory with geoBoundaries files}
}
\value{
Data frame with province assigned to all assets
}
\description{
For assets without province assigned, uses spatial matching with ADM1 boundaries
based on coordinates (if available) or municipality lookup (if no coordinates).
Province names are ASCII-normalized for consistency.
This is a convenience wrapper that loads boundaries from base_dir.
}
\details{
Assign province to assets based on coordinates or municipality (from base_dir)
}
\examples{
\dontrun{
assets <- read_assets("tests/tests_data")
assets_with_provinces <- assign_province_to_assets(assets, "tests/tests_data")
}
}
