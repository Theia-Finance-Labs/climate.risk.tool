% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils__read_inputs.R
\name{assign_state_to_assets}
\alias{assign_state_to_assets}
\title{Assign states to assets using spatial matching (loads boundaries from base_dir)}
\usage{
assign_state_to_assets(assets_df, base_dir)
}
\arguments{
\item{assets_df}{Data frame with asset information}

\item{base_dir}{Base directory containing areas subdirectory with geoBoundaries files}
}
\value{
Data frame with state assigned to all assets
}
\description{
For assets without state assigned, uses spatial matching with ADM1 boundaries
based on coordinates (if available) or municipality lookup (if no coordinates).
State names are ASCII-normalized for consistency.
This is a convenience wrapper that loads boundaries from base_dir.
}
\details{
Assign state to assets based on coordinates or municipality (from base_dir)
}
\examples{
\dontrun{
assets <- read_assets("tests/tests_data")
assets_with_states <- assign_state_to_assets(assets, "tests/tests_data")
}
}
