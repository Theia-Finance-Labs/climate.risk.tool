% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geospatial__downscale_hazards.R
\name{downscale_hazard_rasters}
\alias{downscale_hazard_rasters}
\title{Downscale hazard rasters}
\usage{
downscale_hazard_rasters(hazards, factor = 8L, fun = "mean")
}
\arguments{
\item{hazards}{Named list of \code{terra::SpatRaster} as returned by \code{load_hazards()}}

\item{factor}{Positive integer aggregation factor (e.g., 2, 4, 8, 16). Each
block of \verb{factor x factor} cells is summarized into one cell.}

\item{fun}{Aggregation function to apply; either a function or one of
\code{"mean"}, \code{"median"}, \code{"max"}, \code{"min"}. Defaults to \code{"mean"}.}
}
\value{
Named list of \code{terra::SpatRaster} with same names as input and lower
resolution. Extent and CRS are preserved.
}
\description{
Create lower-resolution versions of hazard rasters by aggregating
cells using a summary function (default: mean). This preserves the full
spatial extent and CRS while reducing the number of rows/columns by the
aggregation factor. Useful for faster experimentation and testing.
}
\details{
Downscale hazard rasters by aggregating to lower resolution
}
\examples{
\dontrun{
base_dir <- system.file("tests_data", package = "climate.risk.tool")
hz <- load_hazards(file.path(base_dir, "hazards"))
hz_ds <- downscale_hazard_rasters(hz, factor = 8)
}
}
