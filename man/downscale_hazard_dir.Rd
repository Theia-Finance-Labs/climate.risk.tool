% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geospatial__downscale_hazards.R
\name{downscale_hazard_dir}
\alias{downscale_hazard_dir}
\title{Downscale all hazard rasters in a directory tree}
\usage{
downscale_hazard_dir(
  input_dir,
  output_dir,
  factor = 8L,
  fun = "mean",
  overwrite = FALSE
)
}
\arguments{
\item{input_dir}{Directory containing hazard rasters (possibly in subfolders)}

\item{output_dir}{Output directory to write downscaled rasters; will be
created if it does not exist}

\item{factor}{Positive integer aggregation factor (e.g., 2, 4, 8, 16)}

\item{fun}{Aggregation function or one of \code{"mean"}, \code{"median"}, \code{"max"}, \code{"min"}}

\item{overwrite}{Logical; whether to overwrite existing files}
}
\value{
Invisibly returns \code{output_dir}
}
\description{
Reads all \code{.tif} rasters under \code{input_dir}, aggregates them by
\code{factor}, and writes them into \code{output_dir} mirroring the subdirectory
structure and filenames.
}
\examples{
\dontrun{
base_dir <- system.file("tests_data", package = "climate.risk.tool")
in_dir <- file.path(base_dir, "hazards")
out_dir <- tempfile("hz_ds_")
downscale_hazard_dir(in_dir, out_dir, factor = 8)
}
}
